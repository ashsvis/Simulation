namespace Simulator.Model.Common
{
    public enum ProjectChangeKind
    {
        Clear,
        Load,
        AddModule,
        RemoveModule,
        AddUnit,
        RemoveUnit,
        AddField,
        RemoveField,
        AddBlock,
        RemoveBlock,
    }
}
